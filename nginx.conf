# nginx.conf

server {
  listen 80;

  location / {
    root   /usr/share/nginx/html;
    index  index.html index.htm;
    # Эта строка важна для SPA: если файл/директория не найдены, отдать index.html
    try_files $uri $uri/ /index.html;
  }
}